import { RegisterForm } from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Créer un compte — Votre Application',
  description: 'Rejoignez Votre Application et commencez gratuitement',
};

export default function RegisterPage() {
  return <RegisterForm />;
}